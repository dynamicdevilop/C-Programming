/* 
        Question - 09 : 3.0 - 1 will be 
            (a)   Integer
            (b)   Floating-Point Number
            (c)   Character
 */

// Solution :-

#include <stdio.h>

int main()
{
    printf("3.0 - 1 will be Floating-point Number.\n");
    printf("Technically it should be double because it is a decimal value.\n");
    printf("But it can also be floating-point number which is also decimal value of less precision.");
    return 0;
}