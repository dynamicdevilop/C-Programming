// C Program to Print an Integer (Entered by the User)

#include <stdio.h>

int main() {
   int number = 0;
   
   printf("Enter a number : ");
   scanf("%d",&number);
   
   printf("Number = %d", number);

    return 0;
}
